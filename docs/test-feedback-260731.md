Okay, so starting the test feedback. One of the first bugs I've seen is for rec01. I logged it as an ad hoc core. I then went into the tag speakers and the verify speakers hasn't run so yes there's no meeting but they haven't loaded to check the speakers from the voice library. So we need to check on rec01 if that did happen because one of them is me, James writing, and I would think that it would maybe be my voice from the samples.

Next issue that I found is that the gutter is still appearing on a smaller window. So even though we've taken some of the things out, I don't understand why the left hand gutter increases as the window is smaller. Now to move on to the test feedback. Hopefully the deploying will work and I can test properly. So let's have a look at PR62 I believe I have tested that. So PR62 I think is okay. The stepper can now go all the way up past 8, so that looks good. See, I think 62 is what we can pass PR62.

PR63 LUSPLIT a recording. So I select the recording, hit split and split at the five minute mark. Okay, an issue that we do have is the text for the minute, second, second is still free text, whereas it should be boxes that could be edited rather than putting in free text. Because this forces me to write the colon rather than hit know that it's minutes. So I hit split. It's trimming it. And it okay so that's good. So it does show the right source device, however it still shows as imported rather than oh maybe that's not the end of the world. So then it moves to transcribed And the two meetings are split. So what I'm now going to do is then try and merge the meetings. So actually let's just check that they did split. So yes, it looks like it's split right in the middle of the transcript which is good. So I'm going to try and now merge them all back together. Cool, that has merged and I can see that the calendar icons which are for I think if related to PR, but the calendar icons are appearing on the merge slide. So yep, it looks like the split works pretty well. And yeah they are all individual files so PR63 passes and can be merged in.

PR64 Yeah, so I have been testing PR64, I think, just throughout and that does make sense. I've also marked a meeting as ad hoc in merge while it is dialising the speakers and that looks like it doesn't allow that which might be okay but it's worth checking the logs on the latest merged number. The merged text also doesn't work with the new recording name I've just shown the rec 00. So if I open it up to see what the actual file name is. Yeah it just truncates the file name. So I think there needs to be some look at when the merge file happens, how it shows the red file number.

Now that the dialisation has happened I'm going to click add hoc, add up hop call, and I so the ad hoc call for the mixing for the merge meeting line button doesn't work on the merge level So confirm speaker protection. Open a transcript where every speaker's confirmed. Yep, that is correct. It's working pretty well now I think. So open a transcript rename speaker so that's the transcript and see where the history button is. Oh there's the history button. No earlier snapshots. Okay so let's go in and look at ones where it did do it. So in recall one, it's an ad block before. Or we can just look at rec 00. And history. Okay, I'm not sure exactly what's happened there. Ah, I think I can see what's happened. The split, which is on PR63, has duplicated the first part. So RECO1 there's then RECO1 Part 1 and RECO1 Part 2. So that isn't a split. They've duplicated then split whereas RECO1 should be replaced as part 1 and only moved down to 5 minutes and then the larger one done up. So there's something not working on the split logic there. The transcript history of the Git, I can see the icon but I can't see any history in it so I think that's worth checking. Have any of the files since this was put in stored a snapshot or a git history because it says high doc saves one for each speaker changing action from now on and I can't actually see any so I don't think that the Git transcription history passes.

Next up 3.16 the calendar permission So if I want to go think it in models and then we want to change the calendar if I change it to Google Calendar doesn't have a pop-up, but that's probably because it is already Okay, the calendar permissions. Yeah, I think we have to pass that. The Git availability I can't see, so when I go to it says there's no snapshot or no earlier snapshots. I think it means checking if there are any snapshots for any of the transcripts that we've recently done because it doesn't look like it's saving stuff using a gear history. Diarisation quality PR65, yep that can go. I haven't tested Kimmy or Groc but we can cross that when we need to. It does feel like the models are performing better. I think we do need to investigate in a future thing after we've merged all of these PRs. The model UI because it is now very cluttered and it's not really clear if which one's selected. There's still ones that say pick one to actually check and be representative of what is being used. The hugging face credential I can the links work to the pages and then you it works pasting it in, it goes in as a password text where I can't see the text. And then when you save it truncates and it saves in the with a tick on the home phase access. What probably should then happen is the text box should then be closed with just the password text redacted in there and then if you want to remove it you click remove otherwise you could type in more things and then hit save again maybe it would overwrite but I don't think that's a particularly good UI. I'm just checking if it is in. I'm running the bash command now. Okay, and security file and generic password. Yep, so there are no hits in the app and it is saved in the keychain so that passes. I'm going to click remove now. Yep, that removes it as not set. And then I can put the password back in and save and it goes in there. Perfect. The calendar no match. I believe this is because we've overwritten this with the add hot call logic. This also the meeting column has also I think been overwritten. It works well. It now has an ability with the blue icon to search or the green icon if it's locked in with the time on the same row and then add hot core with a yellow orange icon that then moves to an orange icon and add hot core when it goes in. I obviously can't tell if when the logic is running properly with the extra diorisations after those are locked in, but I think you've investigated those before.

Okay, so hopefully this is now working again. I tested all of the Hugging Face stuff, that works. That's passed. The only change would be that the model when you save the key, you can still then type in a new key to overwrite or save over it. I think it should be a locked field if there's a key and it just showed a password or adapted text, and then you have to hit remove to clear it.

I also tested the bash command, and it wasn't stored in the app. Calendar no match, I think that has been fixed and the meeting column is now working a lot better with the on-demand and the ticking the cross. Deploy dialogue, we just need to check ongoing so PR66 has passed. PR67 looks like the one word phantom speaker is past.

So I think yes we need to know sixty-seven. I'm just testing 60 Rec 88 now and yes it does allow it. Goes up to 16 speakers, so what I then want to do after it's rediarised is to bring it down to nine, but it doesn't look like it's got the logic in there of the number of speakers of who's accepted and who's not. So there might be some clashing there. It also looks like the names of the different people are working well, and I can see them underneath. I think a key on the enroll the attendees would be useful. What's the difference between purple, green tick and grey on the different pills? That may be a tooltip when you hover over the icon of the two people on the side of that section.

So yeah, I think we can get PR 68 merged in. There's lots of work been done on that. So I think yeah, let's get all of this merged in. Any fixes made and look to get it merged and deployed to main. 