# UI
PyQt6>=6.6
# Embedded browser for the Plaud cloud sign-in (Google/Apple SSO). If it can't
# be installed on a given box, the Plaud sign-in dialog still works via a manual
# token-paste fallback, so the import is guarded.
PyQt6-WebEngine>=6.6

# USB (shared with Windows-Script)
pyusb>=1.2
libusb-package>=1.0

# Transcription (whisper.cpp — no PyTorch needed)
pywhispercpp>=1.2
certifi
mutagen>=1.47

# Mic trigger (Windows audio session API)
pycaw>=20240210
comtypes>=1.4

# Embedded CLI pane (PTY backend, ConPTY on Windows)
pywinpty; sys_platform == "win32"

# Build
pyinstaller>=6.0
