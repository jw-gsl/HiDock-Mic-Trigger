<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened-runtime entitlements required to notarize a bundle that ships
         relocatable Python venvs (numpy / pyusb / pywhispercpp native libs).
         disable-library-validation lets the Developer-ID-signed Python load
         pip-installed libraries that we didn't sign with the same Team ID.
         The other two cover CPython's executable-memory use and venv relocation.
         Trim these if `notarytool log` shows they're unnecessary. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
