# Agent Instructions

See [`CLAUDE.md`](./CLAUDE.md) — that file is the source of truth for agent instructions in
this repo (workflow rules, build/deploy steps, architecture notes, pipeline model backends).
This file exists only so tools that look for `AGENTS.md` specifically find it.

Note: `Windows-Script/AGENTS.md` is a separate, narrowly-scoped file with setup steps for the
Windows USB extractor — it does not supersede this pointer.
